# 風見鶏プロジェクト 地上管制ステーション（GCS）UI要件定義・仕様書

本ドキュメントは、「風見鶏（Kazamidori）プロジェクト」における完全自律型・地上管制ステーション（Ground Control Station: GCS）のUI再設計に向け、プロジェクトの概要、現行UIの構造、要求仕様、および今後のUX改善項目をまとめたものです。Google Stitchを用いたUIデザインおよびデザインシステム（`DESIGN.md`）構築のインプットとして使用します。

---

## 1. プロジェクト概要

### 1.1 背景と目的
モデルロケットの打ち上げ（特に「種子島ロケットコンテスト」等の実戦）において、風（気象条件）は機体の軌道および着地点を左右する最大の要因です。
本プロジェクトは、勘や経験に頼らず、気象データ、物理シミュレーション、最適化アルゴリズムを統合し、現場オペレータに対して**【絶対に失敗しないGO/NO-GO判断】**と**【最適な発射パラメータ（仰角・方位角）】**をリアルタイムに提示する地上管制ステーション（GCS）ソフトウェアの開発を目的としています。

### 1.2 コア機能
1. **確率的軌道・着地点シミュレーション**：風速や推力の不確実性を考慮し、モンテカルロ法を用いて着地点の確率分布（90% CEP楕円、KDE等高線）を算出し可視化する。
2. **ハイブリッド気象モデリング**：広域気象予測（GPVデータ）と、現場に設置した自作の気象観測ステーション「Koinobori（ESP32搭載）」の実測値（5Hz風況、0.1Hz気圧等）をリアルタイムに融合・補正する。
3. **発射パラメータ自動最適化**：指定された目標（定点滞空、高度最大化、有翼、自由）を満たすように、焼きなまし法（SA）やOptuna（ベイズ最適化）を用いて、最適なランチャーの仰角と方位角を自動探索する。
4. **完全オフライン対応**：通信が不安定な打ち上げ現場において、事前取得したオープンストリートマップ（OSM）のタイル画像と現地の磁気偏角データ（WMMモデル）を用い、オフライン環境下でメートル単位の2Dマップを描画する。

---

## 2. UI基本構造とレイアウト

現行システムは、PySide6を使用したレスポンシブな3カラム（または分割型）のドッキングレスレイアウトを採用しています。

```
+------------------------------------------------------------------------------------+
| メニューバー（File / Simulation / View / Help）                                      |
+------------------------------------------------------------------------------------+
| ツールバー（▶ Run | ⏹ Stop | 進捗バー | フェーズ状態表示）                         |
+------------------------------------------------------------------------------------+
|                  | [右上1] 3D軌道プロファイル      | [右上2] 2Dマップ（オフライン対応）     |
|                  | - 軌道の3次元プロット          | - 発射点 (0,0) を中心とするENUマップ    |
|                  | - 北（奥）固定、仰角制限       | - KDE等高線、CEP 90%楕円、散布図       |
| [左カラム]        | - スクロール/ドラッグによる視点移動| - Ctrl+クリックでの発射点移動          |
| パラメータ設定    | - Shift+ホイールでの時間再生   | - 500m / 1000m 捜索支援リング          |
| ＆結果パネル      +---------------------------------+----------------------------------+
|                  | [右下] 風況アナライザー（2分割表示）                              |
|                  | - 左：幾何高度ごとの風速・風向プロファイル（デカルト座標）          |
|                  | - 右：高度別の風向ベクトルを示すコンパス（極座標）                  |
|                  | - 下：風速のリアルタイム履歴（時系列ステップ表示）                  |
+------------------------------------------------------------------------------------+
| ステータスバー（Ready表示 / Koinoboriテレメトリ風速・風向 / 気圧・気温）                |
+------------------------------------------------------------------------------------+
```

---

## 3. UIコンポーネント詳細・要求仕様

### 3.1 左パネル：パラメータ設定＆結果表示（AppState連動）

#### 🚀 機体パラメータ (Airframe Tab)
* **目的**：機体の寸法・質量を入力し、重心（CG）や慣性モーメントを自動計算する。
* **主要入力項目**：
  * Nose Cone (形状、長さ、底面半径、質量、単体重心位置)
  * Body Tube (外径、内径、長さ、質量、配置位置)
  * Fins (枚数、根元長、先端長、スパン幅、後退距離、厚み、質量、配置位置)
  * Internal Components (パラシュートや電装などの質量、重心)
  * バックファイア遅延時間（手入力必須、空欄時はシミュレーション実行をエラーブロック）
* **機能要件**：
  * OpenRocketファイル (`.rkt`) の直接インポート。
  * `Dry質量`、`重心位置`、`慣性モーメント`の自動算出とAppStateへの反映。
  * ファイル操作時の例外処理（`try-except`）および変更検知による「Modified（保存未完了）」フラグ管理。

#### 📍 発射条件設定 (Launch Settings Tab)
* **目的**：ランチャーの物理的な配置・初期設定を行う。
* **入力項目**：
  * 発射地点の緯度 (Latitude)、経度 (Longitude)
  * 地上高度 (Elevation / QNH気圧キャリブレーションと連携)
  * 初期ランチャー仰角 (Elevation Angle)
  * 初期ランチャー方位角 (Azimuth Angle)
  * **磁気偏角 (Magnetic Declination)**：自動取得の理論値を使用するか、オペレータによる手動上書き（マニュアルオーバーライド）を行うかをトグルで切り替え可能。

#### ⚙️ シミュレーション詳細設定 (Advanced Settings Dialog)
* **目的**：シミュレーション精度や環境パラメータの調整（ポップアップダイアログ）。
* **設定項目**：
  * 風速の不確実性 (Wind Uncertainty)
  * 推力の不確実性 (Thrust Uncertainty)
  * 着地確率 (Landing Probability / デフォルト90%)
  * モンテカルロ試行回数 (MC Runs / 練習・通常計算用は50、最終解析時は100〜200)
  * パラシュート抗力係数 (Cd)
  * 推進力ON/OFF時のCdカーブ設定（CSVロード、プレビューダイアログ、クリア機能）
  * 評価関数の係数カスタマイズ（定点滞空評価式: `A * (r_max - r) + B * t` の係数 A, B のカスタマイズ）

#### 🎯 飛行モード選択 (Launch Mode)
* **目的**：ミッションに応じた最適化アルゴリズムの挙動と結果表示項目の変更。
* **選択肢**：
  * **定点滞空**：目標着地点（半径50m以内）に着地しつつ、滞空時間を最大化する。
  * **高度**：到達高度を最大化する。
  * **有翼**：滑空用ペイロードなどがある場合の制御モード。
  * **自由**：最適化制限なしのシミュレーション。
* **表示連動**：モード切り替え時に、下部の「Simulation Results」に表示されるスコア項目（スコア、最大高度、水平ドリフト距離、滞空時間など）のレイアウトが自動的に切り替わる。

#### ⬤ GO / NO-GO インジケータ
* **動作仕様**：
  * リアルタイムの風速（Koinoboriデータ）およびモンテカルロシミュレーションの結果から、安全に回収可能なエリア（Target Radius）内に着地する確率を評価。
  * 許容範囲内であれば青/緑で「⬤ GO」、風速超過または範囲外への逸脱確率が高い場合は赤点滅で「⬤ NO-GO」と表示し、オペレータに警告を促す。

---

### 3.2 右パネル：視覚的アナライザー

#### 1) 3D Trajectory Profile (軌道表示)
* **カメラ制限**：3D空間で裏返りによるオペレータの混乱を防ぐため、方位角（Azimuth）の回転を `-90° 〜 +90°` に制限。
* **軸固定**：常に「真北（North）」が画面 of depth 方向（奥行き正）を向くように空間座標を固定。
* **インタラクション**：
  * マウススクロール：3Dカメラの回転。
  * `Shift + スクロール`：シミュレーション時間の再生・巻き戻し（軌道上の現在位置を示す赤色マーカーが移動し、HUDに「Time: X.X s, Alt: Y.Y m」を表示）。
  * 「Reset True North」ボタン：方位角を`-90°`（真北向き）にワンクリックで復帰。

#### 2) 2D Landing Map (オフラインマップ)
* **座標系**：発射台を原点 `(0, 0)` とした「ENU（East-North-Up）ローカル座標系」に統一。
* **オフライン表示**：事前取得ツール (`utils/map_downloader.py`) でキャッシュした `background.png` と `map_meta.json` を読み込んで描画。
  * 地図データ欠落時の**サイレントフォールバック**：クラッシュせず「青色背景＋方眼グリッド」に変更し、マップ中央に **`[WARNING] OFFLINE MAP NOT LOADED: CHECK GPS COORDINATES`** と高コントラストの警告HUDを表示。
* **描画レイヤー (zorderによる重なり順制御)**：
  1. 背景地図（最背面: `zorder=0`, `alpha=0.6`）
  2. モンテカルロ散布図（`zorder=2`）
  3. 捜索支援距離リング（射点中心の半径500mおよび1000mの同心円、破線、`zorder=2`）
  4. 90% CEP 楕円（緑色、`zorder=3`）
  5. KDE 等高線（オレンジ、`zorder=4`）
  6. 目標着地半径（Target Radius、青破線、`zorder=5`）
  7. 公称（名目）着地点（赤丸、`zorder=6`）
  8. 発射台（青星型、最前面: `zorder=10`）
* **インタラクション**：
  * 左クリックドラッグ：マップのスクロール（パン）。
  * マウスホイール：カーソル位置を中心としたズームイン/ズームアウト。
  * `Ctrl + クリック`：マップ上のクリックした座標の緯度経度を計算し、AppStateの発射原点座標へ自動還流・適用する。
  * `Reset View` ボタン（Homeキー）：射点 `(0, 0)` と Target Radius がちょうど収まる画角（限界範囲＋10%マージン）にズームと位置をリセット。

#### 3) Wind Panel (風況グラフ)
* **構成**：
  * **高度別風向・風速ライン**（デカルト座標）：縦軸を高度（m）、横軸を風速（m/s）とし、風向を角度注記で表示。
  * **高度別風向コンパス**（極座標）：高度が高いほど透明度を下げ、線を太くする等の工夫で重なりによる視認性悪化を防ぐ。最新データのみ強調表示。
  * **時系列風速履歴**：リアルタイムデータを受信したタイミングでプロットし、次のデータが届くまでその値を水平な延長線（`---`）で繋ぐことで、通信途絶時も直近の保持データであることを明示する。

---

## 4. 現場運用・システム要件

1. **オフラインマップ事前取得ツール仕様 (`utils/map_downloader.py`)**：
   * ターゲット緯度経度と捜索半径（例：1000m）を入力。
   * OpenStreetMapからズームレベル16〜17の画像を自動取得し、結合・切り抜きして `assets/offline_map/background.png` を生成。
   * **アクセス間隔制限**：OSM of tile利用規約を遵守するため、リクエスト間に必ず `1.0秒` のウェイトを強制挿入。User-Agentに「KazamidoriGCS/2.0」を明示。
   * `map_meta.json` を同時出力（緯度経度、磁気偏角、適用範囲 `extent_meters` 等）。
2. **Koinobori UDP通信仕様**：
   * PC側のUIフリーズを防ぐため、受信処理は専用の `QThread` (例: `UdpReceiverWorker`) で稼働。
   * 受信データ形式：
     * **`W,<風速_m/s>,<風向_deg>`** (5Hz) : アプリのコンパス/メーターを即座に駆動。
     * **`E,<気圧_hPa>,<気温_C>,<湿度_%>`** (0.1Hz) : 環境情報パネルを更新。気圧データは現地高度補正（QNHキャリブレーション）のゼロ点調整に使用。

---

## 5. UI/UXにおける指摘・今後の改善項目

既存のコードと `memo.txt` から抽出された、再設計時に解消すべき課題および機能拡張のリストです。

### 5.1 操作性・レイアウトの改善
* **マニュアルコンフィグ（Manual Config）の入力バグ**：0を入力すると自動的に強制されて `0.0` と書き換わってしまい、非常に入力しづらいUXになっているため、自然な数値入力を許容するよう修正。
* **パラメータ欄のデッドスペースの解消**：パラメータ入力ドックの縦方向に余分な空きスペース（謎の空間）が存在するため、レイヤーの伸縮や要素配置を最適化する。
* **ボタンとファイル名の並列表示**：インポートした機体データやパラシュートのファイル名がUI上に常に見えるようにし、ファイルロードボタンとファイル名を並列配置する。
* **パラシュートファイル名の可視化**：現在ロードされているパラシュートのファイル名を分かりやすく明示する。
* **Apply / Cancel ボタンの追加**：現在、Manual Configダイアログは「Close（閉じる）」のみとなっているため、設定を確定する「Apply」と破棄する「Cancel」に分離し、安全な設定適用プロセスを構築する。
* **推力曲線（Thrust Curve）ボタンの移動**：モーターファイル読み込み等の関連ボタンを、「Motor Specifications（モーター仕様）」エリア内に綺麗に統合する。
* **`.json` 保存機能のバグ修正**：機体構成などを保存するJSON書き出しボタンが正しく機能していないため、ファイル保存ダイアログの挙動を修正する。

### 5.2 視覚的明確さ・グラフィック
* **2Dマップ上の未凡例の同心円**：現在2Dマップに描画されている「凡例のない2つの円（※500m, 1000m捜索リング）」に対し、ガイドラインである旨のラベルや凡例、距離テキスト（「500m」「1kｍ」）を明確に付記する。
* **コンパスの3Dグラフ内描画**：3D軌道表示のいずれかの角（HUD周辺など）に、空間の向きを示す3D方位コンパスまたはインジケータをオーバーレイし、空間把握を容易にする。
* **危険区域（デッドゾーン）の描画**：立ち入り禁止エリアや風下への障害物などの緯度経度境界を設定し、2Dマップ上に危険区域としてハッチングや赤色の半透明ポリゴンで表示可能にする。
* **軌道・運動特性グラフの追加表示**：シミュレーション結果として、速度-時間（v-t）グラフ、高度-時間（h-t）グラフ、および2D弾道側視図（高度vs水平距離）を切り替え、またはサブパネルで表示できるようにする。

### 5.3 開発・運用向け追加機能
* **Dev Mode (開発者モード) の実装**：デバッグログの出力切り替えや、物理エンジンに渡す生パラメータのダンプ、計算時間の計測などをワンクリックで有効化できる開発用トグルをUIに追加。
* **事後解析用のデータ保存**：測定された風況データおよび計算結果（軌道、スコア、最適化パラメータ）を1つのアーカイブファイル（またはSQLite）にパッケージ出力し、シミュレーションの振り返りを行えるようにする。
* **事前GPVデータの一括取得UI**：打ち上げ当日の指定時間帯のGPV気象データを事前にまとめてローカルにキャッシュするためのダウンロード用UI。
