1. **Define the Audit Protocol**: Create `.jules/audit_protocol.md` containing the checklist per instructions. (Already done)
2. **Execute the Baseline Audit**: Perform a search for math, unit, coordinate, or redundancy violations in the codebase and output the "Baseline Audit Report", proposing a cleanup plan (but not executing it). (Already done)
3. Output the contents of `.jules/audit_protocol.md` to the user.
4. Output the contents of the `Baseline Audit Report` to the user.
